// Stories have moved to DSMButton.stories.tsx
import type { Meta } from '@storybook/react-vite';
import { DSMButton } from './DSMButton';

export default {
  component: DSMButton,
  tags: ['!dev', '!autodocs', '!test'],
} satisfies Meta<typeof DSMButton>;
